{
  "name": "heritage-sweets-platform-root",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack Indian Sweets & Savouries E-Commerce Platform (React + Node.js)",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "client": "npm --prefix client run dev",
    "server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
